enum WorkoutGoal {
  fatLoss,
  recomp,
  muscleGain,
  metabolic_health,
}

enum WorkoutType {
  strength,
  cardio,
  rest,
}

enum WorkoutDisplayMode {
  active,
  retroactive,
  readOnly,
  completed,
}
