enum WorkoutGoal {
  fatLoss,
  recomp,
  muscleGain,
  metabolicHealth,
}

enum WorkoutType {
  strength,
  cardio,
  rest,
}

enum WorkoutDisplayMode {
  active,
  retroactive,
  readOnly,
  completed,
}
