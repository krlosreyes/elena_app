{
  "name": "elena-app-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "TEST-02 (auditoria pre-produccion 2026-07-11): tests de firestore.rules contra el emulador de Firestore. No forman parte del build de la app Flutter; corren aparte con el emulador de Firebase.",
  "scripts": {
    "test": "node --test rules.test.mjs",
    "test:emulator": "NODE_BIN=$(command -v node) && firebase emulators:exec --project elena-app-2026-v1 --only firestore \"$NODE_BIN --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1"
  }
}
